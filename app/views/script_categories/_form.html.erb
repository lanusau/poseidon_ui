<div class="editform_page">

  <% if @script_category.errors.any? %>
    <div class="errors">

      <p><em>Oops... the following errors were encountered:</em></p>
      <ul>
        <% @script_category.errors.full_messages.each do |m| %>
          <li><%= m  %> </li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-container">

    <%= form_for(@script_category) do |f| %>

      <fieldset>
        <legend> Enter information about this script category</legend>

        <div>
          <%= f.label :name, "Name" %>
          <%= f.text_field :name, {"size"=>20,"maxlength"=>200}%>
          <p class="note">Unique script category name </p>
        </div>

        <%= submit_tag("Save") %>
        <%= submit_tag("Cancel") %>

      </fieldset>

    <% end %>

  </div>

</div>
